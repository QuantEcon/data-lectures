# Per-dataset manifest — schema sketch (PLAN Phase 2 / QuantEcon.manual#108)
#
# STATUS: strawman. This is the shape the pilot (PLAN Phase 8) tests against;
# every field here is provisional until proven by a real migration. Decisions
# validated by the pilot get folded into styleguide/datasets.md (manual#108).
#
# PLACEMENT (strawman): a manifest is a sidecar next to the file it describes,
# named "<full filename>.yml":
#
#     lectures/mpd2020.xlsx        <- the dataset
#     lectures/mpd2020.xlsx.yml    <- its manifest
#
# Why full filename rather than stem: two datasets may share a stem across
# extensions (fig_3.xlsx / fig_3.ods), and a stem-keyed sidecar would collide.
# Why a sidecar rather than a parallel manifests/ tree: the manifest travels
# with the data, so a file cannot be moved or removed without it, and CI can
# assert the pairing with a glob rather than a lookup table.
#
# The manifests are the source for the generated catalog page (PLAN Phase 2),
# which doubles as the public dataset registry.
#
# COMPLETENESS: this file documents the fields that recur across the corpus. It
# is not a closed schema, but since 2026-09-07 the `schema` block IS validated:
# scripts/validate_datasets.py (run by .github/workflows/validate-datasets.yml
# on every PR) checks every manifest's conformance to the rules recorded below
# and, for CSVs, that the committed bytes satisfy their own schema block, via
# builders/_validate.py (#119). Provenance fields stay descriptive — a dataset whose
# provenance needs a field not shown here should add one rather than distort
# itself to fit, and a field that earns its place in several manifests should
# then be documented back here. Two such extensions are already in wide use and
# are described where they apply: `schema.sheets` for multi-sheet workbooks, and
# `schema.read_as` for the pandas read-kwargs a positional read needs. Adding
# the four `source`/`license` fields below closed the last known gap between
# this file and practice (QuantEcon/data-lectures#85).

# ---------------------------------------------------------------------------
# Identity
# ---------------------------------------------------------------------------

# NAMING (policy settled 2026-09-07, QuantEcon/data-lectures#113). The tree is
# flat, so the filename is the whole identity: the served URL's last segment
# and the future `qeld.url('<filename>')` key. Name a file once, by these rules:
#   1. name the VARIABLE(S), not the source and not the lecture
#      (gdp_growth_annual, unemployment_rate_annual, private_credit_to_gdp);
#   2. add FREQUENCY when the same variable plausibly exists at another
#      frequency (unemployment_rate_annual beside a monthly one);
#   3. a COMPOSITE is named for what unites its members — if that is only one
#      lecture, name the topic, not the lecture file (us_business_cycle_monthly,
#      not business_cycle_data);
#   4. NO SOURCE PREFIX (no wdi_/fred_) — provenance is `source:` and CATALOG.md;
#   5. NEVER coverage or vintage in a dynamic snapshot's name (it changes on
#      refresh); a verbatim release keeps the upstream's own name (mpd2020.xlsx);
#   6. rename FREELY while `consumers: []`; once adopted, only as a NEW filename
#      with the old one kept serving — a rename breaks every consumer and every
#      downloaded notebook. Selection ("these nine economies") lives in the
#      manifest, not the name.
filename: gdp_growth_annual.csv         # must match the sidecar's own name
title: World Bank GDP growth (annual %) — selected economies
description: >
  Annual real GDP growth for nine economies, extracted from World Bank series
  NY.GDP.MKTP.KD.ZG.

# Exactly one of: verbatim | constructed | dynamic-snapshot
#   verbatim         third-party file republished as distributed
#   constructed      built from upstream sources by our processing
#   dynamic-snapshot constructed, tracking a moving source (FRED, World Bank)
class: dynamic-snapshot

# ---------------------------------------------------------------------------
# Provenance
# ---------------------------------------------------------------------------

source:
  name: World Bank national accounts data, and OECD National Accounts data files
  series: NY.GDP.MKTP.KD.ZG
  url: https://data.worldbank.org/indicator/NY.GDP.MKTP.KD.ZG
  # Dataset DOI where the source issues one, else null. Prefer a DOI that
  # resolves to the DATA deposit; an article DOI is better than nothing, but say
  # which it is. In use by 16 of 33 manifests.
  doi: null
  # The upstream's own version identifier, quoted as the source states it — an
  # edition ("Maddison Project Database 2020"), a vintage stamp, or a file
  # header. Null where the source is genuinely unversioned; say so rather than
  # inventing a version. In use by 10.
  version: null
  citation: >
    World Bank, World Development Indicators, series NY.GDP.MKTP.KD.ZG
    (GDP growth, annual %).
  # Anything a reader must know before trusting these bytes that the fields
  # above cannot carry — most often that the file is NOT what its name or its
  # cited paper implies. Where that is the case, say it here AND at the top of
  # the manifest. In use by 20.
  note: null

license:
  name: CC BY-4.0
  url: https://datacatalog.worldbank.org/public-licenses#cc-by
  # This repo is a stability cache, not a content host: the question is whether
  # the source may be cached and served publicly WITH attribution, not whether
  # we may republish it as our own. A cheap binary gate (AGENTS.md, "Licensing
  # and attribution") — a fast yes for public data sources, blocking only the
  # genuinely restricted ones (e.g. FRED third-party series).
  redistribution: permitted             # permitted | restricted
  verified: 2026-07-16                  # when, and against what, this was
                                        # established — a licence claim with no
                                        # date is not evidence. P1 found the
                                        # authority is often not the project
                                        # homepage but its Zenodo/DOI record.
  # What `redistribution` rests on, whenever that is anything less obvious than
  # a named public licence: a split answer across providers, an inherited
  # exposure, a term found only in prose, or a reasoned judgement about what is
  # actually published here. A bare `permitted` beside a `name: null` is the
  # case that most needs this field. In use by 16.
  note: null

retrieved: 2024-04-10                   # ISO date the bytes were obtained, or
                                        # null for inherited-undated files —
                                        # never invent one from git history.
                                        # A null here MUST be paired with an
                                        # integrity.upstream note (AGENTS.md,
                                        # "Two inherited-file states").
maintainer: QuantEcon                   # who fixes this when it breaks

# ---------------------------------------------------------------------------
# Integrity (PLAN Phase 7) — record the status honestly, don't fake it
# ---------------------------------------------------------------------------
# One permanent axis: is this file actually what the upstream source says?
# (How to check, by class, lives once in AGENTS.md, not per manifest.)
#
# Migration safety — does the file match what the consuming lecture used before
# a repoint? — is deliberately NOT a manifest field. It is a one-time check that
# lives in the repoint PR (a Phase 8 gate), and the sha256 below is what makes
# it reproducible at any later date. P1 (lingcod) proved the two questions get
# different answers: byte-perfect copy of what the lecture consumed, upstream
# provenance unrecoverable.

integrity:
  sha256: null                          # of the committed file
  upstream:
    status: unverified                  # verified | spot-checked | diverged |
                                        #   unverifiable | unverified | failing
    date: null                          # ISO date of the last check
    against: null                       # what a verified check compared to (URL,
                                        # DOI, or repo path) — makes "verified"
                                        # auditable rather than a bare assertion
    note: null                          # ONE line, only when status is not a
                                        # clean verified — e.g. why a file is
                                        # unverifiable. An honest `unverifiable`
                                        # with a note is a known status the
                                        # catalog can show; silence is not.

    # --- `diverged` only ------------------------------------------------
    # `verified` means "checked, and it matches". A file that was checked and
    # found to DIFFER has no honest home in the rest of this vocabulary:
    # `failing` implies something is broken, and `verified` is a lie a reader
    # would act on. `diverged` is the third outcome — checked, differs, and the
    # difference is known and accepted (PLAN "Repoint rules" 3, register #39).
    #
    # It is not a defect state. A migration deliberately moves the copy the
    # lectures already consume, so a known delta against today's upstream is an
    # expected result, recorded rather than resolved mid-migration.
    delta_kind: null                    # local-edit | upstream-moved
                                        #   local-edit    our copy was modified;
                                        #                 upstream is unchanged
                                        #   upstream-moved a newer vintage exists;
                                        #                 adopting it means a NEW
                                        #                 filename (see AGENTS.md
                                        #                 "Corrections vs vintages")
                                        # The two look alike and need opposite
                                        # responses, so the register needs them
                                        # distinguished machine-readably.
    delta: null                         # what differs, precisely enough that
                                        # resolving it later needs no
                                        # re-investigation
    delta_evidence: null                # how delta_kind was ESTABLISHED rather
                                        # than assumed — ruling out the other
                                        # kind is the whole work, and an
                                        # unevidenced claim here misclassifies
                                        # the file's class as well as its status
    register: null                      # the issue tracking the resolution,
                                        # e.g. QuantEcon/data-lectures#39

# ---------------------------------------------------------------------------
# Shape — what a consumer can rely on; also the basis for PR validation tests
# ---------------------------------------------------------------------------

# The schema block is an EXECUTABLE CONTRACT: the shared manifest-driven
# validate() (QuantEcon/data-lectures#119) reads it as its spec, both in every
# dynamic builder's validate stage and in the PR-validation workflow. The three
# decisions that make it executable were settled 2026-09-07 — #120 (`pattern`),
# #121 (nulls), #122 (dtypes) — and are recorded beside the fields below.
#
# What the validator reads: the RAW file as written, with pandas defaults —
# honouring `delimiter` (countries.csv is semicolon-separated) and `read_as`
# (positional workbook reads), with no `index_col`, and with `parse_dates` on
# every column declared `datetime64`. The manifest describes the bytes on
# disk, not the frame a particular lecture builds from them.

schema:
  format: csv
  columns:
    # dtype (#122): the canonical vocabulary is what pandas 3 reports for a
    # default read — `str`, `float64`, `int64`, `bool`, `datetime64` — plus the
    # storage types a non-CSV format genuinely carries (`float32`, `int32`,
    # `int8` from Stata). `string`, `object` and `datetime` are retired (swept
    # 2026-09-07). The validator compares by FAMILY, not exact string: str /
    # string / object are one text family, datetime64 matches any unit, and a
    # nullable bool read back as object matches `bool` — so the check stays
    # green on pandas-2 readers (lecture-datascience, Pyodide). A CSV carries
    # no dtypes, so a declaration is a statement about a default read; a
    # `datetime64` declaration is also the instruction to parse that column.
    - {name: economy, dtype: str, description: ISO3 country code}
    - {name: Country, dtype: str, description: Country name}
    # pattern (#120): a column entry carries EITHER `name` (one exact column)
    # OR `pattern` (a regex), never both. `pattern` is for files whose column
    # set grows — one column per year here — where enumerating them would
    # hard-code an end year into a file whose purpose is to move. Semantics:
    #   - Python `re`, full-match (`YR\d{4}` cannot match `YR19601`);
    #   - the validator walks `columns` IN ORDER and a pattern consumes the
    #     maximal run of CONSECUTIVE matching columns in that position —
    #     column order is part of the contract (positional reads exist);
    #   - at least ONE column must match, else "wide part missing" fails;
    #   - EXHAUSTIVE: every file column is claimed by exactly one entry, so an
    #     unexpected column fails;
    #   - exactly one CAPTURE GROUP marks the date token, from which the
    #     validator derives `date_range` for a wide file. A stricter grid
    #     check (no gaps) is the builder's, layered on top.
    # First used by business_cycle_data.csv.yml (now gdp_growth_annual.csv.yml,
    # 2026-09-01); proposed in QuantEcon/data-lectures#14.
    - {pattern: 'YR(\d{4})', dtype: float64, description: Annual GDP growth (%), one column per year from YR1960}
  row_count_floor: 5                    # PR validation fails below this. For a
                                        # frozen historical extract set it to the
                                        # exact row count by design (P1 lingcod =
                                        # 82); a loose floor is for files that grow.
  date_range: {start: 1960, end: null}  # null end = tracks the moving source

  # Nulls (#121). Two forms, defaulted by `class`:
  #
  # `known_nulls: {column: N}` — an EXACT count; `n != N` fails. Required for
  # every nulled column of a `verbatim` or `constructed` file (P1 found a real
  # one: lingcod's F/Fmsy is null in its terminal assessment year, which a
  # blanket no-nulls rule would reject). A dynamic snapshot may also declare
  # an exact count for a column whose nulls are stable by construction
  # (gdp_growth_annual's YR1960: growth is undefined in the first year).
  # A column with no entry must have no nulls -- every column of a frozen
  # file, and the LABEL columns of a dynamic snapshot (economy, Country, DATE:
  # the placement rule below governs only its series).
  # There is deliberately NO ceiling form ({max: N}): a ceiling silently
  # accepts a column emptying out below the bound, which is the failure the
  # check exists to catch.
  known_nulls: {}                       # e.g. {F_over_Fmsy: 1}
  # `nulls:` — a PLACEMENT rule, required for `dynamic-snapshot` because its
  # counts move on every refresh while WHERE a null may sit does not. It
  # mirrors what both dynamic builders already assert by hand:
  #   along:   rows | columns   which way a series runs — down a time index
  #                             (one series per column) or along a run of
  #                             pattern columns (one series per row)
  #   leading: true | false     empty before a series' first observation
  #   recent:  N                the newest N periods may be unpublished
  #   ended:   [series, ...]    series that stopped; empty after their last
  #                             observation (a historical FRED series)
  #   inner:   {series: [P, ...] | {sparse_until: P}}
  #                             accepted holes INSIDE a series: listed
  #                             periods, or an irregular early history
  # A null anywhere else fails. See us_business_cycle_monthly.csv.yml for the
  # full form and gdp_growth_annual.csv.yml for the wide-file form.
  # nulls: {along: columns, leading: true, recent: 2, ended: [], inner: {}}
  #
  # `known_nulls_total: N` is legal ONLY inside a `sheets:` entry read with
  # `header: null` (assignat.xlsx, dette.xlsx): a positional read has no
  # column names to count against, so an exact total is the only form. It is
  # not a ceiling and not for named columns.

# ---------------------------------------------------------------------------
# Consumers — machine-readable, so a correction knows what to rebuild
# ---------------------------------------------------------------------------
#
# Keep this current: it is how a fix-in-place finds the lectures to rebuild, and
# how we know whether a file may be renamed or removed. Empty list = nothing
# consumes it yet.

consumers: []
  # - repo: QuantEcon/lecture-python-intro
  #   file: lectures/business_cycle.md
  #
  # A consumer that does NOT yet read this repo — one still pointing at a local
  # copy — is listed anyway, because the question this field answers is "what
  # must be rebuilt if these bytes change", not "who is repointed". Say so in a
  # `note`, which is what makes the divergence machine-readable rather than a
  # YAML comment nobody can check, and cite the issue tracking the repoint with
  # a FULLY QUALIFIED reference, since a bare `#N` or a workspace shorthand
  # means nothing when read from this repo:
  #
  # - repo: QuantEcon/test-actions-lecture-intro
  #   file: lectures/french_rev.md
  #   note: >
  #     Reads a local `datasets/` copy, not this file.
  #     Tracked in QuantEcon/workspace-lectures#46.
  #
  # The generated catalog surfaces `note` beside the consumer, so a reader of
  # CATALOG.md's "Used by" column cannot mistake a local-copy reader for a
  # repointed one.
  #
  # Dynamic snapshots only (sketched 2026-09-01, AGENTS.md "Refresh, break, or
  # schema change"; acted on by the Phase 5 consumer fan-out, not yet built).
  # What a merged refresh does for this consumer:
  #   rebuild   dispatch the repo's build; the figures re-render and nothing
  #             on the page narrates a specific number
  #   review    open an issue in the repo carrying the builder's overlap
  #             summary, because prose quotes a value or a row count that the
  #             refresh may have moved
  # Omit for static datasets. A refresh never changes the published schema
  # (that is a new filename), so this routes VALUE changes only.
  #
  # - repo: QuantEcon/lecture-python-intro
  #   file: lectures/business_cycle.md
  #   on_refresh: review

# ---------------------------------------------------------------------------
# Builder — required for constructed and dynamic-snapshot; omit for verbatim
# ---------------------------------------------------------------------------

builder: builders/business_cycle.py      # path to the committed builder, or null

# AGENTS.md says a constructed dataset without its builder is a bug — but the
# repo has inherited several (PLAN Phase 9), and P1's own pilot file is one.
# The tempting workaround is to misclassify them as `verbatim`, which buries
# the gap. This field keeps it visible in the generated catalog instead.
# `unrecovered` is for inherited files only; a NEW constructed file must ship
# its builder (AGENTS.md, "Two inherited-file states").
builder_status: committed               # committed | committed-frozen | unrecovered | not-applicable

# Dynamic snapshots only. Drives the scheduled refresh-as-PR (PLAN Phase 5).
cadence: annual                         # e.g. daily | weekly | monthly | annual
